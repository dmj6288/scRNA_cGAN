#!/bin/bash -l

#SBATCH -N 1 --ntasks-per-node=6 --partition=gpu
#SBATCH --gres=gpu:1
#SBATCH --job-name="PAIR"
#SBATCH --output=/home/dennis00/scRNA_GAN/slurm_archive/GAN_train_%j.out

module load cuda/11.7

conda activate scgpt_2

cd $SLURM_SUBMIT_DIR

cd /home/dennis00/scRNA_GAN/GAN_control

python3 GAN_train.py

echo "SLURM Job ID        : ${SLURM_JOB_ID}"
